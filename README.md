This helps you create a context in Oracle. It allows you to define a variable within a session and assign a value to it.

In other words, you can use variables inside a view, and it enables you to create secure user-specific variables in PL/SQL.
